multiple dependent condition (if-elif-else)

#################
11-07-2024
#################


Iteration(repeation)
->sorting

#component
(initialization , condition, updation)

for loop
->knowlegede about the sequence

while loop
->no knowlegede about the sequence
->no knowlegede about size of data
initialization
while(condition):
		#working
		updation

range function 
->range(start,end,step)

end variable
->print("message",end="seperater") 

#################
12-07-2024
##################

COLLECTION DATATYPES
----> LIST
	*collection of any type of data
	*list is mutable (change,update and delete)
	*list follow insertion order , means list is an ordered collection.
	*list may contain duplicate element
	*list stored in Square [] bracket.
	
	operation of list
	creation,traverse, insertion, updation, delete, sorting, slicing, searching, length, concatenation
	 '*' is used to print the list without the formatting
	
---->Dictionary
	* delete the last key-value pair from dictionary using  dictionaryname.popitem()
	* pop method take only one and last argument for ex: my_dict.pop('Name','Age') it only delete the Age element
	* items() : return a list of items each item in the form of tuple (key and values)  [dict_name.items()]